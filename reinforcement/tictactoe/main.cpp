#include <iostream>
using namespace std;

int main{
	//determine if bot vs bot or bot vs input and who goes first.
	//determine if we will load bot training from previous training interval
	//set training interval, or number of games to be run between prompts
	//run training game x times according to interval
	//show change in probabilities, prompt to continue
	//if n, ask to save training.
}